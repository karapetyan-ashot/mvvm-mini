<Window x:Class="EasySoftware.MvvmMini.WpfDemo.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySoftware.MvvmMini.WpfDemo"
        mc:Ignorable="d"	
        Title="{Binding Title}" Height="450" Width="800">
	
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="4*"/>
		</Grid.ColumnDefinitions>
		<StackPanel Orientation="Vertical">
			<Button Content="Open window" Command="{Binding OpenWindowCommand}" />
			<Button Content="Open window with params" Command="{Binding OpenWindowWithParamsCommand}" />
			<Button Content="Open dialog" Command="{Binding OpenDialogCommand}"/>
			<Button Content="Handle window closing" Command="{Binding OpenHandleWindowClosingCommand}" />
			<Button Content="Handle window loading" Command="{Binding OpenHandleWindowLoadingCommand}" />
		</StackPanel>
		<TextBlock Text="{Binding InfoMessage}" Grid.Column="1" />
	</Grid>
</Window>
